
# This is the project reference for your Supabase project on supabase.com.
# It was automatically set by `supabase link`.
project_id = "yhjwdoiaafaajhlsxoyt"

[api]
port = 54321
schemas = ["public", "graphql_public", "treinamento"] # Adicionado o schema treinamento
extra_search_path = ["public", "extensions", "treinamento"] # Adicionado o schema treinamento
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 17

[studio]
port = 54323

[inbucket]
enabled = true
port = 54324
smtp_port = 54325
pop3_port = 54326

[storage]
enabled = true
file_size_limit = "50MB"

[auth]
enabled = true
# O schema de autenticação é sempre 'auth'.
# O schema 'treinamento' é para seus dados.

[functions]
# Configurações para todas as Edge Functions.
# Por exemplo, para verificar JWT em todas por padrão:
# verify_jwt = true

# Você pode substituir por função, como já está fazendo abaixo.
# Exemplo:
# [functions.my-function]
#   verify_jwt = false

[functions.admin-send-recovery-emails]
verify_jwt = false

# ... (outras funções podem ser listadas aqui se precisarem de configurações específicas)

[analytics]
enabled = false
port = 54329
vector_port = 54330
# As configurações de functions foram movidas para uma seção [functions] mais limpa.
# As portas de edge_functions e realtime foram consolidadas ou removidas em versões mais recentes.
# A CLI gerencia isso de forma mais automática.

